package main

import (
    "fmt"
    "math"
)

func distanceFrom(coordA Coordinate, coordB Coordinate) int {
    return 0
}

type Coordinate struct {
    x int
    y int
}

func coordinate(x int, y int) Coordinate {
    return Coordinate{ x: x, y: y}
}